import { MustBeDirective } from './must-be.directive';

describe('MustBeDirective', () => {
  it('should create an instance', () => {
    const directive = new MustBeDirective();
    expect(directive).toBeTruthy();
  });
});
