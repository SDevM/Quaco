import { MustExistDirective } from './must-exist.directive';

describe('MustExistDirective', () => {
  it('should create an instance', () => {
    const directive = new MustExistDirective();
    expect(directive).toBeTruthy();
  });
});
